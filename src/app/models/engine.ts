export class Engine {
  engineNumber: string;
  cylinderCounts: number;
  capacity: number;
  vehicleId: number;

  constructor(
    engineNumber: string,
    cylinderCounts: number,
    capacity: number,
    vehicleId: number
  ) {
  }
}
